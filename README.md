# mule4-circuit-breaker-module
This connector improves on the Mule circuit breaker modules by using Atomic variables instead of Mule OS(To reduce memory usage) and a few other features(That I like :) )
